#include <stdio.h> 


int main() {

	puts("This program demonstrates the functionality of the sizeof operator in C and potentially C++ (since I don't know anything about C++, I can't say for sure)") ; 

	
	size_t integer = sizeof(int) ; 
	size_t doble = sizeof(double) ; 
	size_t lawng = sizeof(long) ; 



	printf("Integer: %lu, Double: %lu, Lawng: %lu\n", integer, doble, lawng) ; 

	return 0 ; 



} 
